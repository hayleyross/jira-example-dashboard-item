/**
 * Corresponds to the preferences declared in TODO
 */
export interface ExamplePreferences {
    name: string;
    refreshInterval: string; // String representation of interval in minutes
    isConfigured?: boolean;
}

